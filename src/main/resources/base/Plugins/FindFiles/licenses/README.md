# fd Notices

The bundled executable comes from conda-forge `fd-find 10.5.0 h18a1a76_0`
(win-64), pinned in the repository's conda lock file.

`LICENSE-MIT` and `THIRDPARTY.yml` are copied unchanged from that package's
`info/licenses` directory. Upstream: https://github.com/sharkdp/fd